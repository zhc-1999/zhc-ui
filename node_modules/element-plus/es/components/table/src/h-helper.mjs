import { h } from 'vue';

function hColgroup(columns) {
  return h("colgroup", {}, [
    ...columns.map((column) => h("col", {
      name: column.id,
      key: column.id
    }))
  ]);
}

export { hColgroup };
//# sourceMappingURL=h-helper.mjs.map
